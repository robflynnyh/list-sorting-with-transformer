[project]
name = "list-sorting-with-transformer"
version = "0.1.0"
description = "A decoder-only Transformer benchmark for sorting digit lists."
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "matplotlib>=3.7",
    "torch>=2.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8",
]
tracking = [
    "wandb>=0.15",
]

[project.scripts]
sort-transformer-train = "list_sorting_transformer.experiment:main"
sort-transformer-eval = "list_sorting_transformer.evaluate:main"
sort-transformer-compare = "list_sorting_transformer.compare:main"
sort-pointer-trace = "list_sorting_transformer.render_pointer_trace:main"
sort-adjacent-trace = "list_sorting_transformer.render_adjacent_trace:main"
sort-pointer-position-probe = "list_sorting_transformer.pointer_position_probe:main"
sort-pointer-position-sequence = "list_sorting_transformer.pointer_position_sequence:main"
sort-pointer-value-from-position = "list_sorting_transformer.pointer_value_from_position:main"
sort-pointer-next-value-position = "list_sorting_transformer.pointer_next_value_position:main"
sort-pointer-next-value-from-position = "list_sorting_transformer.pointer_next_value_from_position:main"
sort-pointer-compare-values = "list_sorting_transformer.pointer_compare_values:main"
sort-compiled-pointer-compare = "list_sorting_transformer.compiled_pointer_compare:main"
sort-residual-pointer-compare = "list_sorting_transformer.residual_pointer_compare:main"
sort-rasp-transfer = "list_sorting_transformer.rasp_transfer:main"
sort-language-model-transfer = "list_sorting_transformer.language_model_transfer:main"
sort-shortcut-credit = "list_sorting_transformer.shortcut_credit_experiment:main"
sort-shortcut-credit-plot = "list_sorting_transformer.shortcut_credit_plot:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
